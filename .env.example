# TaskBoard env template
#
# Backend
BETTER_AUTH_SECRET=replace-with-shared-secret
DATABASE_URL=postgresql://neondb_owner:password@host:port/dbname?sslmode=require&channel_binding=require
CHAT_MODEL_PROVIDER=openai
CHAT_MODEL_NAME=gpt-4o-mini
CHAT_HISTORY_LIMIT=50
OPENAI_API_KEY=sk-your-openai-key
GEMINI_API_KEY=your-gemini-key
MCP_SERVER_URL=http://localhost:9000

# Backend CORS: allowed *frontend origins* (comma-separated, no trailing slash).
# Example: https://piaic71-hackathon-v2-frontend.vercel.app
CORS_ALLOWED_ORIGINS=http://localhost:3000

# Frontend: base URL of the backend (no trailing slash)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# Optional: script helpers
UV_PYTHON=python3.13
UV_CACHE_DIR=.uv-cache
BACKEND_PORT=8000
FRONTEND_PORT=3000
RUN_DB_TESTS=0
